#!/usr/bin/env node
// Placeholder — real implementation lands in Task 22 (prepare-bundle).
// Until then this is a no-op so `npm run build` works at every commit boundary.
console.log('[prepare-bundle] stub — will copy ctxloom-pro into resources/ctxloom-cli/ once Task 22 lands');
